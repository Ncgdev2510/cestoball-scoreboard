# Scoreboard Pro — Resumen rápido (cliente)

**Qué necesitás:** Windows 64 bits, el instalador que te enviamos y el archivo **`license.lic`** hecho **para tu PC** (no sirve copiarlo de otra máquina).

---

### Instalación

1. Ejecutá el instalador (`.exe`) y completá el asistente.  
2. Si Windows muestra una advertencia de seguridad, solo continuá si confiás en el origen del programa.

### Licencia (`license.lic`)

Colocá el archivo en **una** de estas carpetas:

- Junto a **`Scoreboard Pro.exe`** (carpeta donde quedó instalado el programa), **o**
- `%APPDATA%\scoreboard-pro\` — en el Explorador de archivos podés pegar esa ruta en la barra de direcciones.

El nombre del archivo debe ser exactamente: **`license.lic`**

### Primera vez que abrís la app

- Si aparece **Activación requerida**: copiá el **ID de hardware** con el botón **Copiar**, enviámelo y te mandamos el `license.lic` correcto. Después de guardarlo en una de las carpetas de arriba, pulsá **Reintentar**.
- Si entra directo al panel, ya está activado.

### Marcador en segunda pantalla

Para ver solo el marcador: abrí la app con el parámetro **`?view=scoreboard`** en la dirección (o usá el acceso directo que te indiquemos).

### Cambiaste de computadora

Necesitás un **nuevo** `license.lic` (nuevo ID de hardware). Escribinos con el ID que muestra la pantalla de activación.

---

*Guía completa: `GUIA-USUARIO.md` en la documentación del producto.*
